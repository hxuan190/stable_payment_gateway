# Blockchain Integration

This directory contains blockchain-specific implementations.

## Structure

- `solana/` - Solana blockchain integration (wallet, listener, parser)
- `bsc/` - BNB Chain (BSC) blockchain integration
- `types/` - Common blockchain types and interfaces
